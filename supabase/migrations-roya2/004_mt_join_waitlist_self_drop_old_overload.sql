-- ============================================================================
-- ROYA 2.0 — migration 004: mt_join_waitlist_self_drop_old_overload
-- הוחל בפרודקשן ב-11.9.2026 (project facehdfqvppxnmdtpzuo, version 20260911162508).
-- קובץ זה נוסף לתיקייה המקומית ב-12.9.2026 (משימת "סנכרון SQL") — לא היה קיים לפני כן.
-- ============================================================================

-- תיקון מיידי: ה-CREATE OR REPLACE הקודם יצר overload חדש (8 פרמטרים) במקום להחליף את הפונקציה הקיימת,
-- כי הוספת 2 פרמטרים משנה את "חתימת" הפונקציה מבחינת פוסטגרס. זה השאיר שתי גרסאות קיימות בו-זמנית
-- (6 פרמטרים + 8 פרמטרים עם ברירת מחדל) - מצב שגורם לשגיאת "אי-בהירות" בכל קריאה קיימת מהאתר החי עם 6 פרמטרים.
-- מוחקים את הגרסה הישנה (6 פרמטרים) כדי שתישאר רק גרסה אחת (8 פרמטרים, עם ברירת מחדל NULL לשתיים החדשות -
-- קריאה עם 6 פרמטרים בדיוק כמו היום ממשיכה לעבוד זהה).
drop function if exists public.mt_join_waitlist_self(uuid, text, text, text, text, date);
