# קביעת תור מיידית: מה בוצע ומה ממתין (25.9.2026)

**ההחלטה (רועי, 25.9.2026):** תור שנקבע באתר נכנס מיד ליומן. בעל העסק מקבל עדכון בוואטסאפ לידיעה, לא לאישור, ויכול לשנות או לבטל בעצמו. אין יותר שלב אישור/דחייה לתור חדש, גם לא לתור מרשימת המתנה. תזכורות ואישור הגעה של הלקוח ("מגיע"/"לא מגיע") לא השתנו.

## מה כבר בוצע

| מקום | שינוי | קומיט |
|---|---|---|
| roy-landing (אתר השיווק) | כל הדפים, הדמו, החוזה (PDF + תצוגה) | `c69657f`, `204edbc` (באוויר) |
| salon-alon (אתר הדמו) | אחרי הזמנה: "התור כבר ביומן. לפני התור תישלח אליך תזכורת בוואטסאפ." | `efdca38` (נפרס ל-salon-alon-demo.netlify.app, 25.9) |
| salon-demo | 4 טקסטים ב-siteConfig.js, הכלל ב-CLAUDE.md, FEATURE-PARITY #1 | `46413ec` (נדחף, לא נפרס) |
| roya-owner-app | הוסרו כפתורי "אשר תור"/"דחה", המונה "ממתין לאישור שלך", וההודעות ללקוח שנשלחו רק מאישור/דחייה. תווית הסטטוס "נקבע". 25/25 בדיקות, build תקין | `f9cc408`, `b28912b` (נפרס ל-roya-owner-app.netlify.app, 25.9) |

שורות `pending` ישנות ממשיכות לחסום את השעה שלהן באפליקציה, כך ששום נתון קיים לא נשבר. בבסיס הנתונים (25.9) יש תור אחד בלבד, והוא כבר `confirmed`, כך שאין צורך במיגרציה או בניקוי.

## ממתין לך

### 1. ✅ בוצע (25.9, באישור רועי): שינוי שדה אחד ב-n8n
Workflow: `BASIC - Multi-tenant Booking (confirm/reject)`, id `Ejiq4eCgMmlNcrsL`.
בצומת **Insert Appointment**, בשדה ה-JSON Body, להחליף:
```
status: 'pending', source: 'website'
```
ב:
```
status: 'confirmed', confirmed_at: $now.toISO(), source: 'website'
```
ואז Publish. בלי השינוי הזה, כל תור מהאתר נכנס כ-`pending` ונשאר כך לתמיד, כי ענף האישור מושבת מאז שנמחק Green-API (6.9).
- הגרסה הטיוטה שלא פורסמה שונה מהגרסה הפעילה רק בטקסט של פתק (Sticky Note), כך שהפרסום בטוח.
- גיבוי מלא של ה-workflow לפני השינוי: `roy-landing-private-notes/claude/n8n-backup-BASIC-Multi-tenant-Booking-Ejiq4eCgMmlNcrsL-25-9-2026.json`.

### 2. תבניות וואטסאפ ב-Meta
**חדשה, מחליפה את `owner_new_booking_approval`:**

**`owner_new_booking_notice`**
- קטגוריה: Utility · שפה: he
- Header: (ללא)
- Body: `תור חדש נקבע ונכנס ליומן: {{1}} בתאריך {{2}} בשעה {{3}}. לשינוי או לביטול, אפשר לעדכן ביומן.`
  - דוגמאות: {{1}}="מיכל כהן, תספורת", {{2}}="10.9.2026", {{3}}="16:00"
- כפתורים: אין

**לא נחוצות יותר (אפשר לא להגיש או למשוך):** `owner_new_booking_approval` (עם כפתורי אישור/דחייה), `customer_booking_declined`.

**`customer_booking_confirmed` נשארת**, ועכשיו היא נשלחת מיד עם קביעת התור. אם עוד לא הוגשה, מומלץ "נקבע" במקום "אושר":
`התור שלך ל-{{1}} בתאריך {{2}} בשעה {{3}} נקבע. מחכים לך!`

כשהתבניות יאושרו, ב-n8n: אחרי Insert Appointment לשלוח `owner_new_booking_notice` לבעל העסק ו-`customer_booking_confirmed` ללקוח, במקום הצומת המושבת Send Interactive Buttons (Owner).

### 3. ✅ בוצעו (25.9): פריסות salon-alon ו-roya-owner-app
```bash
cd C:\Users\Ayalon\salon-alon && npm run build && netlify deploy --prod --dir dist
```
```bash
cd C:\Users\Ayalon\roya-owner-app && npm run build && netlify deploy --prod --dir dist
```
(salon-demo "ממש בשולים" ובמצב סימולציה. בעץ העבודה שלו יש שינויים לא-מחויבים שלא קשורים לזה, לכן לפני פריסה שלו: `git stash`, פריסה, `git stash pop`.)

### 4. חומרי מכירה שעדיין מציגים אישור/דחייה כיתרון
`הצעת-פילוג-פיצרים-3-מסלולים-מתוקן-10-9-2026.md:23`, `מצב-נוכחי-מלא-10-9-2026.md:124`, מחקר המתחרים (שורה 145). אלה מסמכים היסטוריים. כדאי לא להסתמך עליהם בשיחות מכירה.
