# DECISIONS-NEEDED — החלטות שדורשות את רועי

> כל שורה: ההחלטה, ברירת המחדל שנבחרה, למה, ואיפה זה משפיע. הלופ ממשיך עם ברירת המחדל. רועי משנה מה שהוא רוצה.

## פתוחות (session 1, 10.9.2026)

| # | החלטה | ברירת מחדל שנבחרה | למה | איפה משפיע |
|---|---|---|---|---|
| D1 | **המודל בסשן הזה**: הפרומפט אמר "Sonnet 5" אבל הסשן רץ בפועל על Fable 5.1 (המודל שנבחר בלקוח). לפי CLAUDE.md, Fable רק באישור מפורש של תשלום קרדיטים. | המשכתי (הסשן הופעל ע"י רועי, בחירת המודל שלו). | לא ניתן לשנות מודל מתוך הסשן. | עלות. אם זה לא מכוון: להפעיל את הסשן הבא על Sonnet 5 בלקוח. |
| D2 | ~~SIMULATION_MODE=true בעץ העבודה של salon-alon~~ **סגורה — 12.9.2026, אושרה במפורש ע"י רועי כ"קבוע".** נטרול כפתורי/קישורי הוואטסאפ (טולטיפ "זה רק דמו") ב-Contact/FloatingButtons/Footer, יחד עם SIMULATION_MODE=true ב-BookingWizard, הם התנהגות מכוונת קבועה — לא שריד-ניסוי. הוחל commit + push: `7c0febc` על `main` (salon-alon). **פירוט החקירה המקורית (לתיעוד, סבב 3, 11.9.2026):** (1) `git log -10 -- BookingWizard.jsx` מאשר ש-`32e37cb` ("restore real booking/availability calls") הוא אכן ה-commit האחרון שנגע בקובץ — הסתירה אמיתית, לא טעות-קריאה. (2) mtime של הקובץ: 8.9.2026 10:10 — **לפני** ה-HEAD commit הנוכחי (`8766510`, 10.9.2026 15:31), כלומר זה שינוי ישן שיושב לא-מחויב כבר יומיים, לא שינוי טרי. (3) **חשוב:** יחד עם BookingWizard.jsx יש עוד 3 קבצים לא-מחויבים מאותו חלון-זמן מדויק (8.9, 10:10-10:21, כל הארבעה תוך 11 דקות): `Contact.jsx`, `FloatingButtons.jsx`, `Footer.jsx` — כולם הופכים קישורי-וואטסאפ אמיתיים לכפתור שפותח טולטיפ "זה רק דמו" ("הכפתור הזה מושבת כאן בכוונה... באתר האמיתי שלכם, לקוח שלוחץ עליו מגיע ישר לוואטסאפ של העסק"). זו חבילת-שינוי אחת, קוהרנטית, בכוונה ברורה: להפוך את כל האתר ל"בטוח להדגמה" (בלי קליק אמיתי לוואטסאפ, בלי קריאת-רשת אמיתית מהאשף). (4) ה-commit `8766510` עצמו (10.9, מאוחר יותר) **מניח כעובדה** שכפתורי הוואטסאפ ב-FloatingButtons/Footer "כבר נוטרלו עם טולטיפ 'זה רק דמו'" — כלומר עבודה מאוחרת יותר בענף כבר הסתמכה על החבילה הזו כאילו היא בתוקף, אף שמעולם לא בוצע עליה commit. כל הראיות מצביעות על כוונה, לא על שריד-מקרי. **לא שוניתי דבר בעץ הזה (0 stash/checkout/reset/commit), לפי ההוראה המפורשת בתוספת שלא לגעת בעץ הזה בכלל.** | לא נגעתי. ה-worktree `salon-alon-roya2` נוצר מ-HEAD (8766510) שבו הערך `false`, ומשמש לכל עבודת ROYA 2.0. | הוראה מפורשת בתוספת ("לא נוגעים בעץ העבודה הזה בכלל: לא stash, לא checkout, לא reset") + ראיות חדשות (חבילת 4 קבצים קוהרנטית, מוזכרת כהנחת-יסוד ב-commit מאוחר יותר). | האתר החי salon-alon-demo: אם רועי פרס מהעץ עם true, ההזמנות באתר החי לא מגיעות ל-n8n. **עדיין ממתין לתשובת רועי: זה מכוון (המשך חבילת "בטוח-להדגמה") או שרד-בטעות?** |
| D3 | **Supabase branch עולה כסף** ($0.01344/שעה, בערך 36 ₪ לחודש אם נשאר פתוח). | לא יצרתי branch. כל ה-migrations נכתבות כקבצי SQL ב-`salon-demo/supabase/migrations-roya2/` וממתינות. האפליקציה רצה במצב `mock` (נתוני דמה מקומיים) עד שיוחלו. | אסור `confirm_cost`. אין Docker/CLI מקומי. | כל פריט שדורש DB אמיתי מסומן DONE (קוד + migration + בדיקה על mock) עם הערה "migration ממתינה". רועי: לאשר branch (זול) או להחיל ישירות על prod אחרי סקירה. |
| D4 | **gh CLI לא מותקן** ולכן לא יכולתי ליצור repo פרטי `royayalon1905/roya-owner-app`. | repo git מקומי בלבד ב-`C:\Users\Ayalon\roya-owner-app`, ענף עבודה `roya-2-app` (לא main). | אין הרשאה ל-`npm install -g` ולא לפתוח חשבונות. | רועי: ליצור repo פרטי ב-GitHub, ואז `git remote add origin <url>` ו-`git push -u origin roya-2-app`. |
| D5 | **בסיס הענף roya-2-landing** | `qa-audit-9-9-2026` (מכיל fa2f553 = תיקון 054-968-5982). | לפי סעיף 2 בתוספת. | שלב 6. |
| D6 | **כניסה לאפליקציית הבעלים** | Supabase Auth magic-link למייל (לא OTP ל-SMS). במצב mock: מסך כניסה דמו עם בחירת "בעלים / עובד". | SMS OTP עולה כסף ודורש ספק. | 1.1, 1.2. |
| D7 | **workflows קיימים (Win-back, Loyalty, Campaigns, Monthly Report) לא נערכים**. נבנים workflows חדשים `ROYA2 - ...` שמחליפים אותם. | חדשים, נפרדים. הישנים נשארים active:false לתיעוד. | הישנים מבוססי Green-API (אסור), tier-gating ישן (mid/top). עריכת workflow חי (BASIC, Reminders, MID Followups) אסורה בלי אישור. | n8n. רועי: אחרי publish של החדשים, לארכב את הישנים. |
| D8 | **N-7 דוח חודשי: טקסט + קישור לדוח באפליקציה** (לא PDF). | טקסט. | PDF דורש ספק/רינדור; קישור לאפליקציה מציג את אותם מספרים. | N-7. |
| D9 | **N-9 Google Calendar דו-כיווני: איזה אירועים חוסמים** | כל אירוע ביומן ייעודי (`integration_config.google_calendar_id`) נחשב חסימה; ביומן ראשי, רק אירועים שכותרתם מתחילה ב-"חסימה". | מונע חסימת היומן בגלל אירועים אישיים. | N-9. |
| D10 | **מכסות הודעות לפי מסלול** (`settings.monthly_message_limit`) | `TBD` ב-config, ברירת מחדל טכנית 300 (הקיים ב-RPC). | רועי ממלא. | mt_usage_counters, N-2. |
| D11 | **"איך זה עובד (4 מסכים)" בדף** נשאר כמו שהוא. ההדגמה הקיימת "תור שהתבטל מתמלא לבד" נשארת לצד החדשה. | כן. | החלטת רועי 10.9. | שלב 6. |
| D12 | **חסימת מבריז (2.8): סף** | `no_show_count >= 2` (תואם "זיהוי מי שלא הגיע פעמיים"), ניתן לשינוי ב-`settings.block_after_no_shows`. | עקביות עם הדף. | 2.8, 1.7. |
| D13 | **חלון ביטול (2.2)** | 4 שעות לפני התור (`settings.cancel_window_hours`). | ערך שכיח במספרות. | 2.2. |
| D14 | **"לא חזרת מזמן"** | 60 יום (`settings.winback_days`), הודעה אחת, רק עם marketing_consent. | לפי הבריף. | 2.4. |
| D15 | **N-5 נאמנות: ברירת מחדל** | כל 10 תורים completed → הטבה "10% הנחה בביקור הבא" (`settings.loyalty`: `{ target: 10, reward_text }`). | מספר עגול, קל להסביר. | N-5. |
| D16 | **N-6 הפניות: ההטבה** | לממליץ: "20% הנחה בביקור הבא" אחרי הזמנה ראשונה שהושלמה דרך הקישור. בלי כסף. | לפי התוספת. | N-6. |
| D17 | **N-8 תור קבוע: אופק** | 8 שבועות קדימה, job שבועי מאריך. | לפי התוספת. | N-8. |
| D18 | **סימון no_show**: אין היום מקור אמת (MID Followups מסמן completed אוטומטית). | האפליקציה: כפתור "לא הגיע" בכרטיס התור (ידני, ע"י הבעלים). זה מפעיל את `mt_handle_no_show` הקיים. | הדרך היחידה האמינה. | 1.4, 1.5, יכולת #11. |
| D19 | **0.4 "טקסטים חסרים"**: במקור `index.html` המספרים קיימים בתוך `<bdi>` (למשל `ל-<bdi>2</bdi> אנשי צוות`). מה ש-read_page הראה כחסר הוא תוצר של כלי הקריאה, לא של הדף. | לא שיניתי כלום. סומן DONE-no-change. יאומת חזותית בשלב 6 (screenshot). | verify-before-fix. | שלב 6. |

## פתוחות (session 2, 11.9.2026 — עלו מבדיקה בפועל, לא מבנייה)

| # | החלטה | ברירת מחדל שנבחרה | למה | איפה משפיע |
|---|---|---|---|---|
| D20 | ~~roy-landing שלב 6 נסוג~~ **סגורה — 11.9.2026 (סבב 2).** חקירה (git log/diff/reflog/timestamps) הראתה שהפורט הראשון עבד ונבדק, ואז `index.html` אופס במכוון (כנראה `git checkout`/`restore` ידני, לא ניתן לאישור סופי כי אין גישה לתמלול ה-session ההוא) כדי לעבור לשיטת-פורט נקייה יותר (`port_landing.mjs`) — אבל אותו session נגמר לו הזמן לפני שהריץ אותה בפועל. סיבת-השורש המדויקת (למה בדיוק בוצע האיפוס) נשארת השערה מבוססת-ראיות, לא ודאות. הפורט הורץ בפועל הלילה (`PORT_CONFIRM_REAL=1`), 21/21 שלבים, commit `88ef0b3`, נדחף ל-`origin/roya-2-landing`. בדיקת הקבלה (`tools/e2e_roya2_landing.py`) עברה 86/86 מול הדף האמיתי. | בוצע: פורט אמיתי + push לענף (לא merge, לא deploy). | — | שלב 6 DONE (חוץ מ-Lighthouse a11y, לא אומת מחדש הסבב הזה — ראו BUILD-STATE 6.5). |
| D21 | **מספרים לא-עטופים ב-`<bdi>` שנמצאו ב-master** (production חי): "לעד 2 אנשי צוות" ו-"5 תורים שלא הלכו לאיבוד". לא קשור ל-0.4 (D19) שהיה false-alarm; אלו ממצאים אמיתיים חדשים. | לא תוקן (מחוץ למנדט הבדיקה). מתועד כפריט קטן לתיקון עתידי. | עקביות עם כלל הפרויקט למספרים ב-RTL. | roy-landing, פס קטן, לא חוסם deploy של שלב 6 אך כדאי לתקן יחד איתו. |
| D22 | **n8n: צמתי ה-DRAFT מכילים httpRequest אמיתיים** (ל-graph.facebook.com ול-Google), לא no-op. הבטיחות מפני שליחה אמיתית נשענת רק על `active:false` + pinning אוטומטי של `test_workflow`, לא על חסימה מובנית בתוך הצומת עצמו. | לא שונה כלום (מחוץ למנדט). מובא להחלטת רועי. | אם מישהו יפעיל (`active:true`) workflow כזה בטעות לפני שה-Meta App Review אושר — תישלח הודעת WhatsApp אמיתית. | כל 10 ה-workflows של ROYA2. רועי: להחליט אם רוצה שכבת בטיחות נוספת (למשל דגל `DRAFT_MODE` שבודק בקוד ה-node ולא רק activation) לפני שממשיכים לבנות עליהם. |
| D23 | **שני פערי-בדיקה קונקרטיים שנשארו TODO בכוונה** (לא תוקנו כי זו משימת בדיקה, לא בנייה): (א) 2.2 — קסקדת ה-WAITLIST האמיתית (ביטול-בזמן) מעולם לא נבדקה, רק ענף "מאוחר מדי". (ב) N-7 — הדוח החודשי לא הושווה בפועל לשאילתת SQL ידנית כמו שקריטריון הקבלה דורש, למרות שהמספרים נראים עקביים. | ממתין לאיטרציית בנייה הבאה. | קרובים לסיום — לא דורשים בנייה חדשה, רק pin-data/בדיקה נוספת. | 2.2, N-7 ב-FEATURE-PARITY.md. |

## פתוחות (session 3, 11.9.2026)

| # | החלטה | ברירת מחדל שנבחרה | למה | איפה משפיע |
|---|---|---|---|---|
| D24 | ~~פירוט הבאג~~ **סגורה — 11.9.2026 (סבב 2), תוקנה.** ראו commit `dfb1fcb`: `REFERENCE_DOW` קבוע (שלישי) מחליף את `dowOf(date)` האמיתי לצורך צפיפות/סטטוס תורים — "היום" תמיד מקבל 9 תורים עשירים (כולל pending+cancelled), בלי קשר ליום-בשבוע האמיתי. בדיקה חדשה (`npm test`, 19/19) מריצה `buildSeed` עם `now` שהוא באמת יום שישי/שבת/שלישי ומוודאת ששלושתם מייצרים אותו מיקס עשיר. **פירוט הבאג המקורי (לתיעוד):** `roya-owner-app/src/data/mockDb.js` (`buildSeed`, שורות 52-66) מייצר נתוני-דמה תלויי `new Date()` אמיתי — פירוט מדויק (נחקר לעומק, session לילה 11.9, לא תוקן):** לולאת הזריעה עוברת על 60 יום (מ-45 ימים אחורה עד 14 קדימה). **שבת (`dow===6`): מדלגת על כל היום לגמרי (`continue`, שורה 55)** — אין שום תור נוצר, לא היום ולא בעבר/עתיד. **שישי (`dow===5`): `count=3` תמיד** (שורה 56) — ומכיוון שהתנאי `dow===5 ? 3 : ...` בא **לפני** התנאי `d===0 ? 9 : ...` באותו ternary, כשה"היום" בפועל חל ביום שישי, ה"בוסט" המיוחד ל"היום" (9 תורים עשירים) **לא קורה בכלל** — יש רק 3 תורים. גרוע מזה: הקצאת הסטטוסים ל"היום" (שורה 65: `k===0→completed, k===4→pending, k===7→cancelled, else confirmed`) מניחה 9 אינדקסים; עם `count=3` יש רק `k=0,1,2`, כך שה"היום" מקבל **רק 1 completed + 2 confirmed, ואף פעם לא pending ואף פעם לא cancelled**. המשמעות המעשית: **אם מישהו מדגים את האפליקציה ביום שישי, מסך "התורים להיום" לא יראה שום בקשה ממתינה לאישור/דחייה ושום תור מבוטל — שני תרחישי-הדגמה מרכזיים (אישור/דחיית בקשה, טיפול בביטול) פשוט לא יהיו זמינים להדגמה.** אם מדגימים בשבת, מסך "היום" **ריק לגמרי** (אפס תורים) — לתשומת לב: זה עשוי להיות דווקא *נכון עסקית* (מספרות רבות בישראל סגורות בשבת), אז ייתכן שזה לא באג אלא פשוט לא-מכוון-לרעה; שווה שרועי יחליט אם זה רצוי כפי-שהוא. גם `recurring_rules[0].weekday` (שורה 114) נקבע תמיד ל-`dowOf(today)` — כלומר כלל התור-הקבוע לדוגמה "עוקב" אחרי היום שרצים בו, לא קבוע. | לא תוקן באפליקציה (רק תועד). | משפיע ישירות על רושם-מכירה אם מדגימים לבעל-עסק פוטנציאלי בדיוק ביום שישי/שבת. | `roya-owner-app/src/data/mockDb.js` שורות 52-66, 114. רועי: להחליט אם (א) לתקן כך שה"היום" תמיד עשיר (9 תורים, מיקס מלא) בלי קשר ליום בשבוע האמיתי, (ב) להשאיר שבת ריקה בכוונה (עסקית נכון) אבל לתקן רק את שישי, או (ג) להשאיר כמו שזה. משימת קוד קטנה לאיטרציה הבאה. |

## פתוחות (session לילה 11.9.2026 — ביקורת אבטחה, לא תוקנו בכוונה)

| # | החלטה | ברירת מחדל שנבחרה | למה | איפה משפיע |
|---|---|---|---|---|
| D25 | ~~ייבוא CSV בלי הגבלת גודל~~ **סגורה — 11.9.2026 (סבב 2), תוקנה.** תקרה 5,000 שורות / כ-5MB (הראשון שמגיע), עם הודעת-חיתוך ברורה למשתמש (לא כישלון שקט). commit `a1f5bf4`, בדיקות חדשות ב-`npm test` (19/19). **פירוט מקורי (לתיעוד):** ייבוא CSV (#32) בלי הגבלת גודל/מספר-שורות — קובץ ענק שמודבק/מועלה יכול לתקוע את הטאב (פרסור סינכרוני ב-main thread). איזה גבול הגיוני (100/1000/5000 שורות? הגבלת MB?) זו החלטת-מוצר לפי גודל רשימות-לקוחות ריאלי, לא טכנית. | לא תוקן. סומן `TODO` בקוד (`rules.js`/`CustomersScreen.jsx`) לפי כלל #9. | תלוי כמה לקוחות יש למספרה טיפוסית — רועי יודע את זה טוב יותר. | `roya-owner-app/src/lib/rules.js` `parseCustomersCsv`, `CustomersScreen.jsx` `ImportModal`. |
| D26 | ~~שדה CSV עם ירידת-שורה נשבר~~ **סגורה — 11.9.2026 (סבב 2), תוקנה.** `parseCustomersCsv` עבר ל-tokenizer אמיתי, תו-אחר-תו, מודע-לגרשיים על הטקסט השלם — שדה מצוטט עם ירידת-שורה מוטמעת נשמר נכון. commit `a1f5bf4` (אותו commit כמו D25), בדיקה ייעודית חדשה עברה, והבדיקה הקיימת (שם עם פסיק במרכאות) ממשיכה לעבור ללא רגרסיה. **פירוט מקורי (לתיעוד):** ייבוא CSV (#32): שדה עם ירידת-שורה מוטמעת (quoted multiline) נשבר בשקט** — הפרסור מפצל לפי שורות פיזיות לפני פרסור-מודע-למרכאות, כך ש"הערות" מרובות-שורות מייבוא Excel/Google Contacts יתפרקו לא נכון. תיקון נכון דורש שכתוב מלא של הפרסר (tokenizer ברמת-תו) — לא בוצע הלילה כי זו פונקציה טעונה עם התנהגות קיימת (למשל שם עם פסיק במרכאות) שכבר יש עליה בדיקה קיימת, ותיקון-לילה בלי סקירה הוא סיכון. | לא תוקן. סומן `TODO` בקוד. | סיכון לאיבוד/עיוות מידע בייבוא אמיתי, לא סיכון אבטחה. | `parseCustomersCsv`. מומלץ: session ממוקד עם בדיקות חדשות *לפני* נגיעה בפרסר. |
| D27 | ~~אין תקרת שדות בטופס~~ **סגורה — 11.9.2026 (סבב 2), תוקנה.** תקרה 50 שדות + `maxLength=200` לתווית; כפתור "+ שדה" ננעל בתקרה עם הודעה ברורה. commit `4adad08`. תוך כדי בדיקה נחשף ותוקן גם באג אמיתי (לא חלק מ-D27 עצמה): מזהה שדה חדש התבסס על `Date.now()` בלבד וקריסות-בו-זמנית (49 קליקים ברצף בבדיקה) יצרו ID כפול → אזהרת React על key כפול בקונסול. תוקן עם סיומת אקראית. `python tests/e2e_walkthrough.py`: 89/89 כולל "קונסול נקי". **פירוט מקורי (לתיעוד):** טפסים (#30): אין הגבלה על מספר שדות בטופס או אורך תווית/ערך שדה** — סיכון נמוך (רק בעל-העסק המחובר עורך טפסים), אבל שווה תקרה סבירה אם המסך ייחשף אי-פעם לתפקיד פחות-מהימן. | לא תוקן. סומן `TODO` בקוד. | נמוך היום, עולה אם הרשאות ישתנו בעתיד (למשל אם עובד יוכל לערוך טפסים). | `roya-owner-app/src/screens/FormsScreen.jsx` `FormEditor`. |

## פתוחות (session 5, 11.9.2026 — אימות-אמת מול Supabase חי)

| # | החלטה | ברירת מחדל שנבחרה | למה | איפה משפיע |
|---|---|---|---|---|
| D28 | **חוסר GRANT אמיתי שנחשף: `mt_staff` — אין `GRANT SELECT ... TO anon`.** נבדק בפועל: `fetchStaff()` באתר הלקוח (`salon-alon-roya2/src/data/catalog.js`) מקבל **401** אמיתי (`permission denied for table mt_staff`, קוד `42501`) מול הפרויקט החי, בזמן שקריאות מקבילות לאותה טבלה-סוג (`mt_services`, `mt_staff_service_prices`) מצליחות (200) כי יש להן policy מפורש ל-`anon`. המשמעות: **בכל אתר לקוח אמיתי שיחובר ל-Supabase, רשימת הצוות בוויזארד לעולם לא תשקף את הצוות האמיתי** — נופלת בשקט (try/catch) לרשימה הסטטית מ-`siteConfig`, בלי שגיאה גלויה למשתמש/לבעל-העסק. **לא תוקן**: התיקון הנכון הוא `GRANT SELECT ON public.mt_staff TO anon;` (וכנראה גם policy מגבילה כמו ב-mt_services, כדי לא לחשוף שדות רגישים) — זה שינוי-הרשאות/סכמה על ה-DB החי, וגם ה-classifier של Claude Code חסם בפועל כל כתיבה ל-DB החי בסשן הזה (ראה D29), וגם רועי אסר במפורש "migration נוסף" בתוספת הנוכחית. | לא תוקן. מתועד בלבד. | רועי: יש להחליט אם זו "migration חדשה" אסורה, או "תיקון קטן ל-grant" מותר בנפרד — ואז להריץ בעצמו או לאשר לסשן הבא. SQL המדויק הנדרש: `GRANT SELECT ON public.mt_staff TO anon;` (+ לשקול policy דומה ל-`mt_services_public_read` שמגביל לעמודות/שורות בטוחות בלבד, לא כל השדות). | `salon-alon-roya2/src/data/catalog.js` `fetchStaff()`. משפיע על כל אתר-לקוח אמיתי, לא רק salon-alon. |
| D29 | **כל כתיבה ל-Supabase החי נחסמה ע"י ה-classifier של Claude Code בסשן הזה** (2 ניסיונות שונים: `UPDATE mt_clients` על `allowed_origin`/`settings`, ו-`INSERT` נפרד ל-`mt_reviews` — שניהם "Blocked by classifier"). גם קריאת `netlify api getSite` (read-only) על `salon-alon-demo` נחסמה. **השפעה קריטית על משימה 1**: 4 ה-RPC-ים הרלוונטיים (`mt_join_waitlist_self`, `mt_is_phone_blocked`, `get_busy_slots`, וגם `mt_reviews` בעקיפין) נעולים ל-Origin המדויק `https://salon-alon-demo.netlify.app` (`allowed_origin` ב-`mt_clients`) — קריאה מ-localhost נכשלת תמיד ("origin not allowed"), ואי-אפשר לשנות את זה זמנית לבדיקה בלי כתיבה שנחסמה. נבדק גם: האתר החי `salon-alon-demo.netlify.app` **מנותק לגמרי מ-Supabase** (0 קריאות רשת ל-supabase.co בזמן מעבר מלא באשף ההזמנה בדפדפן אמיתי) — זו בנייה ישנה, לא ניתן להשתמש בו כתחליף. **לא עקפתי את החסימה** (לא ניסיתי spoofing של Origin header בדפדפן וכו') — לפי ההנחיה המפורשת של ה-classifier עצמו לא לעקוף. | עצרתי אחרי 2 ניסיונות שונים (לפי כלל "נכשל פעמיים → BLOCKED"). | בטיחות: כתיבה ל-DB חי היא בדיוק סוג הפעולה שה-classifier נועד לתפוס; רועי לא נתן הרשאת-Bash מפורשת לזה מראש. | משימה 1 כולה (#10, #27, #33 באתר) — BLOCKED. #26 אומת חלקית (ראו FEATURE-PARITY). רועי: כדי לפתוח את זה, צריך או (א) להוסיף כלל-הרשאה מתאים ב-`.claude/settings.json` (ראו skill `update-config`) ולתת לי הרשאה חד-פעמית לכתיבת-בדיקה הפיכה, או (ב) לבצע בעצמו את שינויי ה-`allowed_origin`/זריעת-נתונים הזמניים ואני רק אמתת בקריאה. |

## סגורות (החלטות רועי, לא לפתוח)
- בונים את כל 33. אין תיוג סטטוס בדף. הדף לא עולה עד FEATURE-PARITY מלא + אישור רועי.
- ROYA משלמת למטא. שתי תזכורות (T-24h + 09:00 batch).
- ביטול בכל עת. 10 הראשונים חודש חינם. מחירים לא משתנים.
