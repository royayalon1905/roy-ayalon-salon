# הנחיות פרויקט - Salon Demo Family

## הקשר
זהו אחד מ-4 אתרי דמו לסלוני שיער/ברברשופ (salon-demo, salon-organic, salon-glam, barbershop-mens) שנבנים כתבנית לבנה (white-label) לשימוש כלי מכירה. כל הפרויקטים נובעים מ-salon-demo כתבנית-אב.

## ארכיטקטורה - חובה לשמור
- src/config/siteConfig.js הוא מקור האמת היחיד לכל תוכן: פרטי עסק, שירותים, צוות, וה-theme (צבעים כ-CSS variables, פונטים)
- אסור טקסט עברי קשיח (hardcoded) בתוך קומפוננטות - הכל דרך siteConfig
- שמות צבעים חייבים להיות סמנטיים (primary, accent, surface, ink, muted) - לא שמות תיאוריים כמו gold/charcoal/copper
- ה-theme מוגדר בשלושה מקומות שחייבים תמיד להישאר מסונכרנים: src/index.css, src/config/siteConfig.js, ו-index.html (meta theme-color, title, fonts)
- פונט תצוגה (display font) חייב לתמוך בעברית בפועל - לא להניח מידע קודם. לבדוק חזותית (למשל render ל-HTML + screenshot) לפני שבוחרים פונט Google Fonts חדש לקונספט; הרבה פונטים "מגניבים" (Rye, Alfa Slab One וכו') הם Latin-only ונופלים בשקט ל-fallback גנרי כשיש טקסט עברי

## שיטת שכפול לקונספט חדש
תמיד robocopy עם exclude: /XD node_modules .git dist .netlify - לעולם לא העתקה ידנית דרך סייר קבצים. אחרי שכפול: npm install ואז npm run build לאימות.

## Commands

**salon-demo** (React, git-tracked):
```bash
npm run build          # לפני כל deploy
netlify deploy --prod  # פריסה לפרודקשן — רק אחרי אישור מפורש
```
לפני `deploy --prod`: חובה `git status` לוודא שאין שינויים לא-קשורים/לא-מאושרים ב-working tree שיעלו בטעות (ראה Workflow Rules #1, וגם האזהרה המפורטת תחת "זרימת עבודה" למטה).

**roy-landing** (static HTML/JS, לא git-tracked בפריסה):
```bash
netlify deploy --prod  # פריסה ישירה, אין סיכון working-tree
```

**n8n workflows** — אין CLI מקומי. שינויים דרך n8n API/UI. `active: true` = פורסם ורץ; `active: false` = draft, נגיש רק דרך Test URL.

**Supabase** — שינויי סכימה (migrations) רק דרך MCP tools (`apply_migration`), לא ידנית ב-Dashboard בלי תיעוד.

**אין עדיין:** test suite, linter מוגדר. אם נדרש בעתיד — להוסיף כאן, לא להניח שקיים.

## Code Style & Architecture

**Multi-tenancy (Supabase):**
- כל טבלה חדשה שקשורה ללקוחות מקבלת קידומת `mt_` (`mt_clients`, `mt_end_customers`, `mt_appointments`, `mt_usage_counters`). לא לגעת בטבלאות single-tenant ישנות (`public.appointments`) בלי סיבה מפורשת.
- זיהוי לקוח בזמן ריצה תמיד דינמי (`instance_id` → `mt_clients`), לעולם לא hardcoded ולא לפי סדר יצירה.
- RPC functions: תמיד עם `search_path` קבוע (מניעת hijacking).
- RLS מודלק על כל טבלת `mt_`; אם RLS דלוק בלי policy — זה מכוון, לא באג, עד שיש client_id אמיתי לעבוד מולו.
- **תלות policy↔GRANT:** בכל פעם שנוספת policy אמיתית ראשונה על טבלה, GRANT מוגבל-לעמודות נכתב **באותה פעימה**, לא בנפרד — רגרסיה ידועה שכבר קרתה (policy קורא עמודה מסוימת לסינון, GRANT בלי אותה עמודה חוסם את כל השאילתה, לא רק מסנן).

**סודות/טוקנים:**
- לעולם לא בקוד גלוי, לא ב-env vars גלובליים, לא בטבלאות ציבוריות.
- ב-n8n: credential store ייעודי לכל לקוח כשאפשר; אם המגבלה הטכנית (n8n Cloud Starter לא תומך ב-Variables/`$env` דרך API) מונעת זאת — הטוקן נשמר ב-`mt_clients.integration_config` (jsonb) ונקרא דינמית ב-runtime, לא מוטבע בקוד ה-workflow.
- כל פתרון זמני/מתפשר (למשל טוקן מוטבע לצורך טסט בודד) מסומן ב-sticky note/הערת קוד מפורשת עם "TEMPORARY" ותנאי להסרה.

**קופי ותוכן (Hebrew):**
- מחרוזות תוכן תמיד ב-config file ייעודי (`siteConfig.js`), לא hardcoded בתוך קומפוננטות.
- קופי שיווקי בעברית לפי מבנה PAS (Problem-Agitate-Solve), טון גובה-עיניים, בלי מילים גבוהות/פלצניות, בלי ציניות כלפי בעל העסק, בלי אימוג'ים בטקסט קופי (אלא אם התבקש ספציפית).
- מספרים בתוך משפטי RTL (כמו "299₪") עטופים ב-`<bdi>`.

**מוצר:**
- קהל היעד לא-טכנולוגי → פתרון דטרמיניסטי, בלי AI/NLU בליבת התהליך הקריטי (הזמנת תור, אישור/דחייה). AI מותר רק בשכבות נלוות (למשל סינון לידים), לא בליבה.
- כל תרחיש חדש (booking, lead qualification) נבנה כ-workflow **פרמטרי אחד** שמשרת את כל הלקוחות, לא שכפול ידני ללקוח.

## זרימת עבודה
פרומפטים מפוצלים לשלבים (עיצוב ויזואלי → תוכן → פיצ'רים), לא מגה-פרומפט אחד. תמיד build מקומי + בדיקה ויזואלית לפני commit. git flow: git add . → commit → push, ואז netlify deploy --prod. חיבור Git/GitHub נעשה רק אחרי שהעיצוב הראשוני אומת (לא מיד אחרי שכפול).

**אזהרה: `netlify deploy --prod` בונה מה-working tree, לא מה-commit.** אם יש בעץ העבודה שינויים לא-מחויבים שאסור שיעלו (למשל פיצ'ר שנמצא באמצע פיתוח וממתין לתאריך יעד), הם ייכללו בבנייה ויעלו לפרודקשן גם בלי commit/push. לפני כל פריסה כזו: `git stash` את השינויים הלא-רלוונטיים, לפרוס, ואז `git stash pop` כדי להחזיר אותם. (תוקע ב-2026-07-12: שינויי הזמנות עם webhook אמיתי כמעט עלו לפרודקשן ככה — אותה תקרית שמתועדת גם ב-Workflow Rules #1 למטה.)

## Workflow Rules

1. **בדיקת git לפני deploy** — תמיד `git status` לפני `deploy --prod` בפרויקטים git-tracked (salon-demo). אם יש שינויים לא-קשורים/לא-מאושרים ב-working tree — להפריד כירורגית (stash/שחזור סלקטיבי, ראו הפירוט תחת "זרימת עבודה" למעלה), לא לפרוס גורף. (תקרית מתועדת: 2026-07-12.)

2. **אישור מפורש חובה לפני:** deploy לפרודקשן, מחיקה, publish (n8n `active: true`), כל פעולה בלתי-הפיכה. לעולם לא לבצע ולדווח אחרי — לבקש אישור לפני.

3. **verify-before-reporting** — כל טענת "זה עובד"/"זה תוקן" מגובה בבדיקה בפועל (DOM check חי, API call אמיתי, לא רק "הקוד נכתב נכון"). אם בדיקה נכשלת (screenshot timeout וכו') — לעבור לבדיקה חלופית אמינה (JS/DOM), לא לוותר על האימות.

4. **pause-on-observation-dont-fix** — כשמתגלה בעיה/סטייה מהתכנון תוך כדי עבודה, לדווח ולעצור, לא "לתקן תוך כדי" בלי לשאול. חל גם על מבני payload לא-צפויים, שגיאות API לא-מוכרות, וחוסר עקביות בין מה שסוכם למה שקיים בפועל.

5. **לא לנחש מבנה API חיצוני** — אם יש מקור אמיתי ומוכח (למשל workflow קיים ב-n8n שכבר עובד בפרודקשן, או תיעוד רשמי) יש להעתיק/לאמת ממנו, לא לנחש מבנה payload/endpoint.

6. **אין `npm install -g` או שינויי סביבה גלובליים בלי אישור מפורש נפרד.**

7. **בחירת מודל** — ראו סעיף "מודל AI" למטה (Sonnet 5 / Opus 5 / Fable 5).

8. **סודות (טוקנים, מפתחות) מועברים רק בתוך session של Claude Code ישירות, לעולם לא דרך צילומי מסך או בצ'אט הרגיל.** אם סוד נחשף בטעות (למשל בצילום מסך) — יש להחליף/לבטל אותו (revoke) לפני שממשיכים, לא להתעלם.

9. **תיעוד TODO במקום, לא רק בשיחה** — כל פשרה/החלטה זמנית (env var, טוקן מוטבע, קונפליקט webhook לא-פתור) מתועדת ב-sticky note/הערת קוד בתוך הקובץ הרלוונטי, כדי שתשרוד גם אם השיחה נגמרת.

10. **זיכרון:** `verify-before-reporting`, `pause-on-observation-dont-fix`, `memory-hygiene-one-lesson-per-file`, `hebrew-marketing-copy-no-ai-tells` — עקרונות קבועים שחלים על כל משימה בפרויקט הזה, לא רק כשמוזכרים במפורש.

## דברים שכבר תוקנו בתבנית-האב (salon-demo) - לוודא שלא חוזרים
- באג UTC בחישובי תאריך - חובה timezone ישראל מקומי, לא toISOString()
- תיבת הסכמת דיוור חייבת להיות false כברירת מחדל (חוק ספאם ישראלי)
- min-h-screen/max-h-[90vh] הוחלפו ל-min-h-dvh/max-h-[90dvh] (App.jsx, Hero.jsx, BookingModal.jsx) - תוקן ב-2026-07-06 בפועל, לא רק תועד

## תוכן שנוצר עד כה (לא לשכפל בלי לבדוק)
- שורת סטטיסטיקה גנרית ב-Hero (מספר שנות ניסיון / לקוחות / דירוג) עלולה להישמע מלאכותית - לחשוב על מספרים ספציפיים ומבוססים לכל קונספט, או להשמיט לגמרי אם אין תוכן אמיתי
- salon-demo: תמונת "תספורת נשית" בגלריה (id 4, photo-1522336572468) היא בפועל תמונת מסעדה - מומלץ להחליף

## מודל AI
- Sonnet 5 — משימות מכניות/דיבוג/תשתית/קוד/אבטחה (ללא שינוי).
- Opus 5 — ברירת מחדל למשימות יצירתיות/עיצוביות/קומפוזיציה/קופי (הוחלף מ-Fable 5). כלול במסלול Pro/Max של רועי, לא דורש קרדיטים נוספים. מגיע קרוב ליכולות Fable 5 בחלק ניכר מהמשימות.
- Fable 5 — רק כשצריך את המקסימום האפשרי (משימה יצירתית מורכבת/ארוכה במיוחד, או כש-Opus 5 לא נותן תוצאה מספקת), ורק אם רועי מאשר מפורשות תשלום קרדיטים נוספים - כמו שהיה נהוג עד עכשיו.
